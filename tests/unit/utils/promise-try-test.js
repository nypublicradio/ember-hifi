import promiseTry from 'dummy/utils/promise-try';
import { module, test } from 'qunit';

module('Unit | Utility | promise try');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = promiseTry();
  assert.ok(result);
});
