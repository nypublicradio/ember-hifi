import audioErrorTypes from 'dummy/utils/audio-error-types';
import { module, test } from 'qunit';

module('Unit | Utility | audio error types');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = audioErrorTypes();
  assert.ok(result);
});
