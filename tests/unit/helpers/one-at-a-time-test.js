import { oneAtATime } from 'dummy/helpers/one-at-a-time';
import { module, test } from 'qunit';

module('Unit | Helper | one at a time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oneAtATime([42]);
  assert.ok(result);
});
